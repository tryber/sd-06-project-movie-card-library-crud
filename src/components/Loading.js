import React, { Component } from 'react';
import './loading.css';

class Loading extends Component {
  render() {
    return (
      <div className="messageLoading">Carregando...</div>
    );
  }
}

export default Loading;
